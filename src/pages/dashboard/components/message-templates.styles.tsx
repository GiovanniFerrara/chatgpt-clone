import { Box, styled } from "@mui/material";

export const MessagesTemplatesContainer = styled(Box)(() => ({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  marginTop: "20%",
}));
